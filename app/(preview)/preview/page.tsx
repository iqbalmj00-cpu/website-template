export { default, metadata } from "@/app/__preview/page";

// Next 16 requires route configuration to be statically declared in this file.
export const dynamic = "force-dynamic";
export const revalidate = 0;
